name: Update Personal Best Tracker

on:
  # Run automatically at the start of every month
  schedule:
    - cron: '0 0 1 * *'  # 00:00 UTC on the 1st of every month
  
  # Allow manual triggering from GitHub Actions tab
  workflow_dispatch:

jobs:
  update-pb:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GH_PAT }}
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install -r requirements.txt

    - name: Install Playwright browser
      run: |
        python -m playwright install chromium
    
    - name: Run Personal Best Tracker
      run: python pb_updater.py
    
    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add README.md pb_widget.md
        if git diff --staged --quiet; then
          echo "No changes to commit"
        else
          git commit -m "Auto-update Personal Best Tracker - $(date +'%B %Y')"
          git push
        fi

